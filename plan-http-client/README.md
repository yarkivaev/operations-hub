# plan-http-client

JS read client for `operations-hub-plan-http`.

Endpoints: `GET /health`, `GET /api/v1/plan`, `GET /api/v1/timeline`.
Version handshake against `/api/v1/meta` is opt-in (`strictVersion: true`).
